The tf files in this example deploy the following:

A basic Azure landing zone compliant internal load balancer
Using the existing network spoke vnet you supply in tfvars
The resource group must exist first 

The example files have been generated by copilot.
The ones without ALZ in them are generic examples for reference.
You can just rename the example files to .tf files if you wish.
Your own values come from the tfvars file